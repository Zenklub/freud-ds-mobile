import { ITextProps, IHeadingProps } from 'native-base';

export interface TextProps extends ITextProps {
	inverted?: boolean;
}
export interface HeadingProps extends IHeadingProps {
	inverted?: boolean;
}
